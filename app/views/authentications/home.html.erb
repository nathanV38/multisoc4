<% if user_signed_in? %>
 Logged in as <strong><%= current_user.email %></strong>.
 
  <%#= link_to(image_tag("facebook.jpg", :title => "ok"), "CDM.PNG", :class => "grouped_images", :rel => "group1") %>
  <p class="fancybox">ça marche?</p>
  <a class="iframe" href="http://www.example">This goes to iframe</a>
  
 <%= link_to 'Edit profile', edit_user_registration_path %> |
 <%= link_to 'Authentications', authentications_path %> |
 <%= link_to "Logout", destroy_user_session_path, method: :delete %>
 <% if Authentication.find_by_user_id(current_user[:id]) != nil %>
	<%#= raise Authentication.find_by_user_id(current_user[:id])["provider"].to_yaml %>
	connecté par <%= session["provider"] %>
	<%#= raise current_authentication.to_yaml %>
	
 <% else%>
	
	<li><%= link_to "Facebook", user_omniauth_authorize_path(:facebook) %></li>
	<li><%= link_to "Google", user_omniauth_authorize_path(:google_oauth2) %></li>
	
 <% end %>
<% else %>
 <%= link_to "Sign up", new_user_registration_path %> |
 <%= link_to "Login", new_user_session_path %>
 
 <li><%= link_to "Facebook", user_omniauth_authorize_path(:facebook) %></li>
 <li><%= link_to "Google", user_omniauth_authorize_path(:google_oauth2) %></li>

 
 

<% end %>



<script type = "text/javascript">
$(document).ready(function() {

	/* This is basic - uses default settings */
	
	$("a.grouped_images").fancybox();
	$("fancybox").fancybox();
	/* Using custom settings */
	
	$("a#inline").fancybox({
		'hideOnContentClick': true
	});

	/* Apply fancybox to multiple items */
	
	$("a.grouped_images").fancybox({
		'transitionIn'	:	'elastic',
		'transitionOut'	:	'elastic',
		'speedIn'		:	400, 
		'speedOut'		:	200, 
		'overlayShow'	:	false
	});
	
});
</script>